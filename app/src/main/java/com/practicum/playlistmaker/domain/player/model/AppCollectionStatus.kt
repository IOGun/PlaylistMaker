package com.practicum.playlistmaker.domain.player.model

enum class AppCollectionStatus {
    RECEIVED,
    DEFAULT,
    ERROR,
    STATE_DEFAULT,
    STATE_PREPARED,
    STATE_PLAYING,
    STATE_PAUSED
}